{
  "private": "true",
  "name": "example-app",
  "version": "1.0.0",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "postinstall": "cp scripts/react_native_pods.rb node_modules/react-native/scripts/react_native_pods.rb && cd packages/mobile/ios && pod install"
  }
}
